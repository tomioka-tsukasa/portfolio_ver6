import * as styles from './PageTitle.css'

export const PageTitle = () => {
  return (
    <div className={styles.root}>
      <p className={styles.title}>Tsukasa Tomioka.</p>
      <p className={styles.subTitle}>Portfolio Site.</p>
    </div>
  )
}
